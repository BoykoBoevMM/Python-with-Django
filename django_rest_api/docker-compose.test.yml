# version: '3.8'

services: 
  django:
    container_name: django_rest_api_container
    build:
      context: ../
      dockerfile: django_rest_api/Dockerfile
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - 8000:8000
    env_file:
      - .env
    environment:
      - DJANGO_SETTINGS_MODULE=django_server.settings.test_settings
    stdin_open: true
    tty: true
    depends_on:
      - database

  database:
    container_name: django_rest_database_container
    image: postgres:15
    volumes:
      - data:/var/lib/postgresql
    environment:
      POSTGRES_DB: ${TEST_DATABASE_NAME}
      POSTGRES_USER: ${TEST_DATABASE_USER}
      POSTGRES_PASSWORD: ${TEST_DATABASE_PASSWORD}
    ports:
      - ${TEST_DATABASE_PORT}:${TEST_DATABASE_PORT}

volumes:
  data: